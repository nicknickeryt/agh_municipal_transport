#ifndef ENUMS_H
#define ENUMS_H

// Enumeration representing the direction of the transport
enum class Direction { A, B };

// Enumeration representing the type of transport
enum class TransportType { BUS, TRAM };

// Enumeration representing the schedule day
enum class ScheduleDay { WORKDAY, SATURDAY, HOLIDAY };

#endif // ENUMS_H